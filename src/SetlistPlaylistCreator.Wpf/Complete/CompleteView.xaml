<UserControl x:Class="SetlistPlaylistCreator.Wpf.Complete.CompleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SetlistPlaylistCreator.Wpf.Complete"
             xmlns:designersupport="clr-namespace:SetlistPlaylistCreator.Wpf.Complete.DesignerSupport"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance designersupport:DesignTimeCompleteViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">

    <ContentControl Style="{StaticResource ContentControl.PageShellStyle}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Your playlist has been created successfully!"
                       Style="{StaticResource TextBlock.PageTitleStyle}"/>

            <Button Grid.Row="1"
                    Content="Close"
                    Margin="0,16,0,16"
                    Command="{Binding CloseApplication}"
                    Style="{StaticResource Button.PrimaryCenteredStyle}"/>
        </Grid>
    </ContentControl>
</UserControl>
